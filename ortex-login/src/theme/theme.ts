import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
        main: '#D3CFC6',
        },
        secondary: {
        main: '#34acac',
        },
        background: {
            default: '#202020'
        }
    },
});

export default theme